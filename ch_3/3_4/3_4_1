#include <iostream>
#include <string>
using namespace std;
int main()
{
	string s1, s2;
	while (cin >> s1 >> s2)
	{
		if (s1 < s2)
		{
			cout << s2 << endl;
		}
		else if(s1 == s2)
		{
			cout << "the two strings are equal." << endl;
		}
		else
		{
			cout << s1 << endl;
		}
	}
	
	return 0;
}
