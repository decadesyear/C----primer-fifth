int main()
{
	int a;
	vector<int> ivec{ 1,2,3,4,5,6,7,8 };
	//while (cin >> a)
	//{
	//	ivec.push_back(a);
	//}
	for (auto it=ivec.begin();it+1!=ivec.end();++it)
	{
		cout <<(*it+*(++it))<<endl;
	}
	return 0;
}
